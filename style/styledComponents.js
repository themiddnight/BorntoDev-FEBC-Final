'use client';

import styled from 'styled-components'

export const StyledBody = styled.body`
  background-image: radial-gradient(ellipse at top, #1b2735 0%, #000000 90%);
  background-attachment: fixed;
`